# hybridmd

Convert parsed document elements into hybrid Markdown — Markdown wherever it's lossless, sanitized embedded HTML only for tables whose structure (merged cells, nesting, ragged rows) Markdown can't represent.

> **v1 in progress.**
